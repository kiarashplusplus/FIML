name: FIML CI/CD Pipeline (Core Only)

# This workflow runs only the core tests on every commit for fast feedback.
# Component-specific tests run via dedicated workflows triggered by path changes.
# See: test-core.yml, test-providers.yml, test-arbitration.yml, test-dsl.yml,
#      test-mcp.yml, test-agents.yml, test-infrastructure.yml, test-integration.yml

permissions:
  contents: read

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test-core:
    runs-on: ubuntu-latest
    
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
      
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: fiml_test
          POSTGRES_USER: fiml_test
          POSTGRES_PASSWORD: fiml_test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install TA-Lib dependencies
        run: |
          # Try to install TA-Lib from Ubuntu packages first (more reliable)
          sudo apt-get update
          sudo apt-get install -y libta-lib0 libta-lib-dev || true
          
          # If Ubuntu package installation fails, download from GitHub mirror
          if [ ! -f /usr/lib/libta_lib.so ]; then
            echo "Installing TA-Lib from source (GitHub mirror)"
            wget https://github.com/TA-Lib/ta-lib/releases/download/v0.4.0/ta-lib-0.4.0-src.tar.gz
            tar -xzf ta-lib-0.4.0-src.tar.gz
            cd ta-lib/
            ./configure --prefix=/usr
            make
            sudo make install
            cd ..
          fi

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Initialize database schema
        env:
          PGPASSWORD: fiml_test_password
        run: |
          psql -h localhost -U fiml_test -d fiml_test -f scripts/init-db.sql

      - name: Lint with ruff
        run: |
          ruff check fiml/core/ fiml/cache/

      - name: Type check with mypy
        run: |
          mypy fiml/core/ fiml/cache/
        continue-on-error: true

      - name: Run Core tests
        env:
          FIML_ENV: test
          REDIS_HOST: localhost
          POSTGRES_HOST: localhost
          POSTGRES_DB: fiml_test
          POSTGRES_USER: fiml_test
          POSTGRES_PASSWORD: fiml_test_password
          # Mock Azure OpenAI configuration for tests
          AZURE_OPENAI_ENDPOINT: https://mock-azure-openai.openai.azure.com/
          AZURE_OPENAI_API_KEY: mock-api-key-for-testing
          AZURE_OPENAI_DEPLOYMENT_NAME: gpt-4
          AZURE_OPENAI_API_VERSION: 2024-02-15-preview
        run: |
          pytest -v --cov=fiml.core --cov=fiml.cache --cov-report=xml --cov-report=term --no-docker \
            tests/test_core.py \
            tests/test_cache.py \
            tests/test_cache_improved.py \
            tests/test_cache_optimizations.py \
            tests/test_cache_comprehensive.py \
            tests/test_cache_async_operations.py

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: core

  # build:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   if: github.ref == 'refs/heads/main'

  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Clean runner caches
  #       run: |
  #         sudo apt-get clean || true
  #         rm -rf ~/.cache/pip || true
  #         rm -rf ~/.cache || true
  #         docker system prune -af || true

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v2

  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v2
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           fiml/fiml:latest
  #           fiml/fiml:${{ github.sha }}
  #         cache-from: type=registry,ref=fiml/fiml:latest
  #         cache-to: type=inline

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main'

  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Deploy to production
  #       run: |
  #         echo "Deployment step - configure based on your infrastructure"
  #         # Example: kubectl apply -f k8s/
  #         # Example: helm upgrade fiml ./charts/fiml
